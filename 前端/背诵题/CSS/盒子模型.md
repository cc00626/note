**Content (内容)**：存放文本、图片等的实际区域。

**Padding (内边距)**：内容与边框之间的透明区域。

**Border (边框)**：包裹在 padding 之外的线。

**Margin (外边距)**：盒子与其他盒子之间的距离（透明）。



### 标准盒子模型

宽度（width）仅指内容的宽度



### IE盒模型

宽度（width）包含了内容、内边距和边框



**`box-sizing: content-box;`**（默认值）：触发标准盒子模型。

**`box-sizing: border-box;`**：触发 IE 盒子模型。