### 1. Error Boundary 原理

React 提供了错误边界（Error Boundary）机制，用于捕获子组件树中的渲染异常。

核心 API：

```
static getDerivedStateFromError(error)
componentDidCatch(error, info)
```

### 2. 标准实现

```
class ErrorBoundary extends React.Component {
  constructor(props) {
    super(props)
    this.state = { hasError: false }
  }

  static getDerivedStateFromError(error) {
    return { hasError: true }
  }

  componentDidCatch(error, info) {
    reportError(error, info)
  }

  render() {
    if (this.state.hasError) {
      return <Fallback />
    }
    return this.props.children
  }
}
```

使用方式

```
<ErrorBoundary>
  <Page />
</ErrorBoundary>
```

Error Boundary 不是万能的，必须明确边界：

它只能捕获：

-  render 阶段错误 
-  生命周期错误 
-  构造函数错误 

它无法捕获：

-  事件处理函数错误 
-  async / await 异常 
-  setTimeout / Promise 内错误 
-  SSR 错误 

同时：

-  只能捕获子组件错误 
-  自身错误捕获不到 

### 统一处理错误体系

#### 1.react层

渲染兜底

#### 2. 页面级

路由维度

#### 3. 运行时

捕获js运行时的错误

```
window.onerror = function (message, source, lineno, colno, error) {
  reportError(error)
}

window.onunhandledrejection = function (event) {
  reportError(event.reason)
}
```

