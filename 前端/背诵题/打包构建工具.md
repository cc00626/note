## Tree Shaking 的概念

- **Tree Shaking** 是一种 **静态分析并移除未使用代码（Dead Code Elimination）** 的技术，主要用于减少最终打包体积。
- 基于 **ES Module 的静态特性**（`import` / `export` 在编译阶段即可确定依赖关系），构建工具（如 Webpack、Rollup、Vite）可以分析哪些代码未被引用，从而安全地移除。



Tree Shaking 本质是 **静态分析 + DCE（Dead Code Elimination）**，核心流程包括：

### **(1) 构建依赖图**

- 基于 **ES Module** 的静态导入特性（`import/export`），构建工具在构建阶段即可生成完整的 **模块依赖图**。
- **静态特性**保证了依赖关系在编译期已知，无需运行时计算。

### **(2) AST 静态分析**

- 工具会将源代码解析为 **AST（抽象语法树）**，并标记哪些导出（`export`）被使用。
- 例如：

```jsx
// utils.js
export const a = 1;
export const b = 2;

// index.js
import { a } from './utils.js';
console.log(a);
```

在 AST 分析后，`b` 未被引用，会被标记为 **unused**。

### **(3) 标记未使用的代码**

- 构建工具遍历 AST，对每个 `export` 做标记：

- 如果有引用 → **used**
- 如果无引用 → **unused**

- 这个过程在 **tree shaking plugin** 或 **compiler** 中完成（Webpack 的 `usedExports`、Rollup 内置）。

### **(4) 删除未使用代码（DCE）**

- 在打包阶段，构建工具根据标记移除未使用的导出。
- **UglifyJS / Terser** 等压缩工具会根据标记的 dead code 进行消除。

#### 为什么 AST 很关键

- **原因**：

- `import` 和 `export` 是声明式语法，能直接从 AST 静态分析中确定引用关系。
- 无需运行代码即可判断哪些变量或函数未被使用。

- **工具示例**：

- Webpack 使用 **acorn** 解析 AST。
- Rollup 使用 **ESTree** 格式 AST。

- **关键点**：

- 如果代码包含动态特性（如 `require(variable)`），AST 分析会失败 → 无法 Tree Shaking。

### **为什么 Tree Shaking 依赖 ES Module**

- **ES Module 的特点**：

- **静态依赖分析**：`import` 和 `export` 语句在编译阶段就能确定模块依赖图。
- 不支持运行时动态导入（除 `import()`），无条件语句导入。

- **CommonJS 的问题**：

- `require()` 是动态调用，依赖无法在编译阶段完全确定，构建工具无法准确识别未使用代码。

- **结论**：为了充分利用 Tree Shaking，必须使用 **ES Module 格式的包**，而非 CommonJS。

## **构建工具如何实现 Tree Shaking**

- **Webpack**：

- 配置 `mode: 'production'`，会自动开启 Tree Shaking 和 Uglify。
- 需开启 `optimization.usedExports` 或 `sideEffects` 配置。

- **Rollup**：

- 天生支持 Tree Shaking，因为只支持 ESM。

- **Vite**：

- 基于 Rollup 实现，天然支持 Tree Shaking。

- **关键点**：

- 要求源码和依赖包均为 ESM 格式。
- 配置 `package.json` 中 `"sideEffects": false`（告知工具哪些文件无副作用）。

#### **确保第三方包支持 ESM**

- 检查 `package.json`：

- **CommonJS 包**：`"main": "index.js"`
- **ESM 包**：`"module": "index.esm.js"` 或 `"exports": { "import": "./index.esm.js" }`

- 如果第三方库只提供 CJS 格式，可以：

- 找到 ESM 版本（如 `lodash-es` 代替 `lodash`）。
- 使用 `babel-plugin-transform-commonjs` 或 `@rollup/plugin-commonjs` 转换，但 Tree Shaking 效果不如原生 ESM。

